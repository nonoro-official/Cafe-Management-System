# Leave this relative so requests stay same-origin: the Vite dev proxy forwards
# /api to the backend in development, and nginx serves the API at /api in
# production. Only set an absolute URL if the API lives on a different origin
# (not recommended — cross-site auth cookies will not be sent).
VITE_API_BASE_URL=/api
